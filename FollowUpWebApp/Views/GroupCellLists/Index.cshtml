@model IEnumerable<FollowUpWebApp.Models.GroupCellList>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Department.DepartmentName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Member.ChurchDataId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Department.DepartmentName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Member.ChurchDataId)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.GroupCellListId }) |
            @Html.ActionLink("Details", "Details", new { id=item.GroupCellListId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.GroupCellListId })
        </td>
    </tr>
}

</table>
