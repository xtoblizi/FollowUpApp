@model IEnumerable<FollowUpWebApp.Models.MessageTemplate>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.MessagingType)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MessageBody)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.MessagingType)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MessageBody)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MessageTemplateId }) |
            @Html.ActionLink("Details", "Details", new { id=item.MessageTemplateId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MessageTemplateId })
        </td>
    </tr>
}

</table>
